// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: email_unsubscribe_code.sql

package notifdb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getEmailByCode = `-- name: GetEmailByCode :one
SELECT email FROM notification_email_unsubscribe_code WHERE code = $1
`

// Rust used fetch_optional: callers must treat pgx.ErrNoRows as "not found".
func (q *Queries) GetEmailByCode(ctx context.Context, code pgtype.UUID) (string, error) {
	row := q.db.QueryRow(ctx, getEmailByCode, code)
	var email string
	err := row.Scan(&email)
	return email, err
}

const upsertNotificationEmailUnsubscribeCode = `-- name: UpsertNotificationEmailUnsubscribeCode :one

INSERT INTO notification_email_unsubscribe_code (email, code) VALUES ($1, $2)
ON CONFLICT (email) DO UPDATE
SET code = notification_email_unsubscribe_code.code
RETURNING notification_email_unsubscribe_code.code
`

type UpsertNotificationEmailUnsubscribeCodeParams struct {
	Email string
	Code  pgtype.UUID
}

// Queries ported from crates/notification_db_client/src/email_unsubscribe_code.rs
// Callers must lowercase the email before calling (Rust did it in-code) and
// generate the uuid v7 code themselves.
// On conflict the existing code is kept and returned.
func (q *Queries) UpsertNotificationEmailUnsubscribeCode(ctx context.Context, arg UpsertNotificationEmailUnsubscribeCodeParams) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, upsertNotificationEmailUnsubscribeCode, arg.Email, arg.Code)
	var code pgtype.UUID
	err := row.Scan(&code)
	return code, err
}
