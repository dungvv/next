// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: pins.sql

package macrodb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getPins = `-- name: GetPins :one
SELECT COUNT(*) as "count"
        FROM "Pin"
        WHERE "userId" = $1
`

func (q *Queries) GetPins(ctx context.Context, userid string) (int64, error) {
	row := q.db.QueryRow(ctx, getPins, userid)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getPins2 = `-- name: GetPins2 :many
WITH PinnedItems AS (
        SELECT
            p."pinnedItemId" as pin_item_id,
            p."pinnedItemType" as pin_item_type,
            p."pinIndex" as pin_index
        FROM "Pin" p
        WHERE p."userId" = $1
    ), Combined AS (
        SELECT
            'document' as "item_type",
            d.id as "id",
            CAST(COALESCE(di.id, db.id) as TEXT) as "document_version_id",
            d.owner as "user_id",
            d.name as "name",
            d."branchedFromId" as "branched_from_id",
            d."branchedFromVersionId" as "branched_from_version_id",
            d."documentFamilyId" as "document_family_id",
            d."fileType" as "file_type",
            d."createdAt"::timestamptz as "created_at",
            d."updatedAt"::timestamptz as "updated_at",
            d."projectId" as "project_id",
            di.sha as "sha",
            NULL::bool as "is_persistent",
            dt.sub_type as "sub_type",
            pi.pin_index as "pin_index",
            CASE 
                WHEN dt.sub_type = 'task' 
                    AND ep_status.values->'value' ? $2
                THEN true 
                WHEN dt.sub_type = 'task'
                THEN false
                ELSE NULL 
            END::bool as "is_completed"
        FROM "Document" d
        LEFT JOIN document_sub_type dt ON dt.document_id = d.id
        LEFT JOIN entity_properties ep_status 
            ON dt.sub_type = 'task'
            AND ep_status.entity_id = d.id 
            AND ep_status.entity_type = 'TASK'
            AND ep_status.property_definition_id = $3
        INNER JOIN PinnedItems pi ON pi.pin_item_id = d.id AND pi.pin_item_type = 'document'
        LEFT JOIN LATERAL (
            SELECT
                b.id
            FROM
                "DocumentBom" b
            WHERE
                b."documentId" = d.id
            ORDER BY
                b."createdAt" DESC
            LIMIT 1
        ) db ON true
        LEFT JOIN LATERAL (
            SELECT
                i.id,
                i."documentId",
                i."sha",
                i."createdAt",
                i."updatedAt"
            FROM
                "DocumentInstance" i
            WHERE
                i."documentId" = d.id
            ORDER BY
                i."updatedAt" DESC
            LIMIT 1
        ) di ON true
        UNION ALL
        SELECT
            'chat' as "item_type",
            c.id as "id",
            NULL::int8 as "document_version_id",
            c."userId" as "user_id",
            c.name as "name",
            NULL::text as "branched_from_id",
            NULL::text as "branched_from_version_id",
            NULL::text as "document_family_id",
            NULL::text as "file_type",
            c."createdAt"::timestamptz as "created_at",
            c."updatedAt"::timestamptz as "updated_at",
            c."projectId" as "project_id",
            NULL::text as "sha",
            c."isPersistent" as "is_persistent",
            NULL as sub_type,
            pi.pin_index as "pin_index",
            NULL::bool as "is_completed"
        FROM "Chat" c
        INNER JOIN PinnedItems pi ON pi.pin_item_id = c.id AND pi.pin_item_type = 'chat'
        UNION ALL
        SELECT
            'project' as "item_type",
            p.id as "id",
            NULL::int8 as "document_version_id",
            p."userId" as "user_id",
            p.name as "name",
            NULL::text as "branched_from_id",
            NULL::text as "branched_from_version_id",
            NULL::text as "document_family_id",
            NULL::text as "file_type",
            p."createdAt"::timestamptz as "created_at",
            p."updatedAt"::timestamptz as "updated_at",
            p."parentId" as "project_id",
            NULL::text as "sha",
            NULL::bool as "is_persistent",
            NULL as sub_type,
            pi.pin_index as "pin_index",
            NULL::bool as "is_completed"
        FROM "Project" p
        INNER JOIN PinnedItems pi ON pi.pin_item_id = p.id AND pi.pin_item_type = 'project'
    )
    SELECT item_type, id, document_version_id, user_id, name, branched_from_id, branched_from_version_id, document_family_id, file_type, created_at, updated_at, project_id, sha, is_persistent, sub_type, pin_index, is_completed FROM Combined
    ORDER BY pin_index ASC
`

type GetPins2Params struct {
	UserId               string
	Values               []byte
	PropertyDefinitionID pgtype.UUID
}

type GetPins2Row struct {
	ItemType              string
	ID                    string
	DocumentVersionID     string
	UserID                string
	Name                  string
	BranchedFromID        pgtype.Text
	BranchedFromVersionID pgtype.Int8
	DocumentFamilyID      pgtype.Int8
	FileType              pgtype.Text
	CreatedAt             pgtype.Timestamptz
	UpdatedAt             pgtype.Timestamptz
	ProjectID             pgtype.Text
	Sha                   string
	IsPersistent          pgtype.Bool
	SubType               NullDocumentSubTypeValue
	PinIndex              int32
	IsCompleted           bool
}

func (q *Queries) GetPins2(ctx context.Context, arg GetPins2Params) ([]GetPins2Row, error) {
	rows, err := q.db.Query(ctx, getPins2, arg.UserId, arg.Values, arg.PropertyDefinitionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetPins2Row
	for rows.Next() {
		var i GetPins2Row
		if err := rows.Scan(
			&i.ItemType,
			&i.ID,
			&i.DocumentVersionID,
			&i.UserID,
			&i.Name,
			&i.BranchedFromID,
			&i.BranchedFromVersionID,
			&i.DocumentFamilyID,
			&i.FileType,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ProjectID,
			&i.Sha,
			&i.IsPersistent,
			&i.SubType,
			&i.PinIndex,
			&i.IsCompleted,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removePin = `-- name: RemovePin :exec
DELETE FROM "Pin" WHERE "userId" = $1 AND "pinnedItemId" = $2 AND "pinnedItemType" = $3
`

type RemovePinParams struct {
	UserId         string
	PinnedItemId   string
	PinnedItemType string
}

func (q *Queries) RemovePin(ctx context.Context, arg RemovePinParams) error {
	_, err := q.db.Exec(ctx, removePin, arg.UserId, arg.PinnedItemId, arg.PinnedItemType)
	return err
}

const removePinByPinnedItemId = `-- name: RemovePinByPinnedItemId :exec
DELETE FROM "Pin" WHERE "pinnedItemId" = $1 AND "pinnedItemType" = $2
`

type RemovePinByPinnedItemIdParams struct {
	PinnedItemId   string
	PinnedItemType string
}

func (q *Queries) RemovePinByPinnedItemId(ctx context.Context, arg RemovePinByPinnedItemIdParams) error {
	_, err := q.db.Exec(ctx, removePinByPinnedItemId, arg.PinnedItemId, arg.PinnedItemType)
	return err
}

const reorderPins = `-- name: ReorderPins :exec
UPDATE "Pin"
        SET "pinIndex" = data."pinIndex", "updatedAt" = NOW()
        FROM (
            SELECT
                unnest($1::text[]) AS "pinnedItemId",
                unnest($2::text[]) AS "pinnedItemType",
                unnest($3::int[]) AS "pinIndex"
        ) AS data
        WHERE
            "Pin"."userId" = $4
            AND "Pin"."pinnedItemId" = data."pinnedItemId"
            AND "Pin"."pinnedItemType" = data."pinnedItemType"
`

type ReorderPinsParams struct {
	Column1 []string
	Column2 []string
	Column3 []int32
	UserId  string
}

func (q *Queries) ReorderPins(ctx context.Context, arg ReorderPinsParams) error {
	_, err := q.db.Exec(ctx, reorderPins,
		arg.Column1,
		arg.Column2,
		arg.Column3,
		arg.UserId,
	)
	return err
}

const upsertPin = `-- name: UpsertPin :exec
INSERT INTO "Pin" ("userId", "pinnedItemId", "pinnedItemType", "pinIndex", "createdAt", "updatedAt")
        VALUES ($1, $2, $3, $4, NOW(), NOW())
        ON CONFLICT ("userId", "pinnedItemId", "pinnedItemType") DO UPDATE
        SET "updatedAt" = NOW()
`

type UpsertPinParams struct {
	UserId         string
	PinnedItemId   string
	PinnedItemType string
	PinIndex       int32
}

func (q *Queries) UpsertPin(ctx context.Context, arg UpsertPinParams) error {
	_, err := q.db.Exec(ctx, upsertPin,
		arg.UserId,
		arg.PinnedItemId,
		arg.PinnedItemType,
		arg.PinIndex,
	)
	return err
}
